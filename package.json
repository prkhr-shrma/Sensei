{
  "name": "sensei",
  "version": "1.0.0",
  "description": "AI-powered DSA coach for Blind 75 — live hints, spaced repetition, and AI-verified solutions powered by Claude",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "concurrently \"node server.js\" \"vite\"",
    "build": "vite build",
    "start": "node server.js",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "gen-token": "node --input-type=commonjs -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\""
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.4.0",
    "express-session": "^1.19.0",
    "helmet": "^8.0.0",
    "passport": "^0.7.0",
    "passport-github2": "^0.1.12",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "@vitest/coverage-v8": "^2.1.0",
    "concurrently": "^9.0.0",
    "supertest": "^7.0.0",
    "vite": "^6.1.0",
    "vitest": "^2.1.0"
  }
}
